export interface ProductItem {
    product_image   : string,
    product_name    : string,
    product_price   : number,
    currency_symbol : string 
}

export interface ProdcutScale {
    large   : number,
    small   : number
}